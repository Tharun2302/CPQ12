# ===========================================
# COPY THIS TO .env FILE IN YOUR PROJECT ROOT
# ===========================================

# ===========================================
# MICROSOFT AUTHENTICATION CONFIGURATION
# ===========================================
# Get this from Azure App Registration
# Go to: https://portal.azure.com > Azure Active Directory > App registrations
# If not set, Microsoft sign-in is disabled; users can still sign in with email/password.
VITE_MSAL_CLIENT_ID=your-microsoft-client-id-here
VITE_MSAL_REDIRECT_URI=

# ===========================================
# DATABASE CONFIGURATION (Optional)
# ===========================================
# For MySQL/MariaDB
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=cpq_database1
DB_PORT=3306

# For MongoDB (if using MongoDB instead)
MONGODB_URI=mongodb://localhost:27017
DB_NAME=cpq_database

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
# For sending emails (Gmail example)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# ===========================================
# HUBSPOT INTEGRATION (Optional)
# ===========================================
# For HubSpot CRM integration
HUBSPOT_ACCESS_TOKEN=your-hubspot-token-here

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port for your backend server
PORT=3001

# Backend URL (for frontend API calls)
# Local: http://localhost:3001
# Production: https://zenop.ai
VITE_BACKEND_URL=http://localhost:3001

# Frontend URL (for email links and redirects)
# Local: http://localhost:5173
# Production: https://zenop.ai
BASE_URL=http://localhost:5173

# Backend Base URL (for CORS and email links)
# Local: http://localhost:3001
# Production: https://zenop.ai
APP_BASE_URL=http://localhost:3001

# JWT Secret for authentication tokens (CHANGE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================================
# DEVELOPMENT vs PRODUCTION
# ===========================================
# Set to 'production' when deploying
NODE_ENV=development

# ===========================================
# MICROSOFT CLARITY ANALYTICS
# ===========================================
# Get your Clarity Project ID from: https://clarity.microsoft.com/
# Create a project and copy the Project ID
VITE_CLARITY_ID=your-clarity-project-id-here

# Enable Clarity in development mode (set to '1' to enable, '0' or leave empty to disable)
# In production, Clarity automatically works (no need to set this)
VITE_CLARITY_ENABLE_DEV=0

# ===========================================
# HOTJAR (session recordings + heatmaps)
# ===========================================
# Numeric Site ID from https://insights.hotjar.com/ > Settings > Sites & Organizations.
# LEAVE BLANK for local dev and in the committed default: blank means the tag never loads
# and no script is requested, so dev traffic never records into the same site as real users.
# Vite bakes this in at BUILD time, so changing it needs a rebuild + redeploy to take effect.
# Set it ONLY in the production/staging build environment.
VITE_HOTJAR_ID=

# ===========================================
# APPROVAL WORKFLOW DEFAULT RECIPIENTS
# ===========================================
# Frontend defaults (Vite requires VITE_ prefix)
VITE_APPROVAL_TECH_EMAIL=
VITE_APPROVAL_LEGAL_EMAIL=
VITE_APPROVAL_DEALDESK_EMAIL=

# Backend defaults (fallbacks if frontend doesn't provide addresses)
TECHNICAL_TEAM_EMAIL=
LEGAL_TEAM_EMAIL=
DEAL_DESK_EMAIL=

# ===========================================
# ZOHO SIGN (Optional second e-signature provider)
# ===========================================
# EVERY VALUE BELOW IS DELIBERATELY BLANK. Fill in only what you need.
# Blank ZOHO_SIGN_ENABLED means Zoho Sign is completely off: no provider
# picker in the UI, no poller, and every /api/zoho-sign/* route returns 503.
# The built-in CPQ e-sign flow is unaffected either way.
# Accepted "on" values: 1, true, yes, on
ZOHO_SIGN_ENABLED=

# Data centre of the Zoho Sign account: us | eu | in | au | jp | ca | sa
# Find it by opening Zoho Sign in a browser and reading the domain
# (sign.zoho.in means "in"). Tokens are NOT portable between data centres.
# Blank defaults to: us
ZOHO_SIGN_DC=

# From the Zoho API Console (https://api-console.zoho.<dc>), Self Client.
# NEVER commit real values. NEVER log them.
ZOHO_SIGN_CLIENT_ID=
ZOHO_SIGN_CLIENT_SECRET=

# One-time exchange of the Self Client grant code. Long-lived: Zoho refresh
# tokens do not expire. Treat this as a password.
ZOHO_SIGN_REFRESH_TOKEN=

# Shared secret entered in Zoho Sign > Settings > Webhooks.
# Blank disables webhook processing entirely (polling still works).
ZOHO_SIGN_WEBHOOK_SECRET=

# Polling fallback. Required while there is no public HTTPS callback URL.
# Blank defaults to: off, 300000 ms, batch 20, 30000 ms minimum refresh.
ZOHO_SIGN_POLL_ENABLED=
ZOHO_SIGN_POLL_INTERVAL_MS=
ZOHO_SIGN_POLL_BATCH=
ZOHO_SIGN_MIN_REFRESH_MS=

# Field coordinate calibration. Zoho does not document the unit or the origin,
# so these are set once, after measuring against a real Zoho account.
# ORIGIN: top | bottom   UNIT: pt | px   Blank defaults to: top, pt
ZOHO_SIGN_COORD_ORIGIN=
ZOHO_SIGN_COORD_UNIT=

# Optional overrides. Leave blank to use the built-in data centre table.
# Set BOTH to reach a data centre the table does not list.
ZOHO_SIGN_API_BASE=
ZOHO_SIGN_ACCOUNTS_BASE=
